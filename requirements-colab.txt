# Colab-friendly install file for the LEDGAR coursework pipeline.
#
# Run this from the repository root in Google Colab:
#   %pip install -q -r requirements-colab.txt
#
# PyTorch is intentionally not listed here because Colab already provides a
# CUDA-compatible torch build. Installing torch from PyPI can replace it with a
# build that does not match the selected Colab runtime.

-e .

datasets>=2.18
huggingface_hub>=0.20
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
ipywidgets>=8.0

transformers>=4.38
accelerate>=0.27
pyyaml>=6.0
wandb>=0.16
